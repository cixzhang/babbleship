

// For now, hard coding the shape of the map.

export const GRID_SIZE = 5;
export const MIN_WORD_LENGTH = 3;

export const ROWS = Array.from({length: 5});
export const COLUMNS = Array.from({length: 5});
